import styled from "styled-components";

function ToastMessage() {
    return <Wrapper>ToastMessage</Wrapper>;
}

export default ToastMessage;

const Wrapper = styled.div`
    position: absolute;
    top: 5rem;
    right: 2rem;
    opacity: 0;
`;
